{
  "author": "c10ckw0rk",
  "description": "Simple script that loads different js and css based on a percentage.",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "browserify": "^14.0.0",
    "gulp": "^3.9.1",
    "gulp-rename": "^1.2.2",
    "gulp-sourcemaps": "^2.4.0",
    "gulp-uglify": "^2.0.1",
    "jasmine-core": "^2.5.2",
    "karma": "^1.3.0",
    "karma-chrome-launcher": "^2.0.0",
    "karma-jasmine": "^1.1.0",
    "karma-phantomjs-launcher": "^1.0.2",
    "karma-verbose-reporter": "0.0.3",
    "phantomjs-prebuilt": "^2.1.14",
    "pump": "^1.0.2",
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0",
    "watchify": "^3.9.0"
  },
  "license": "ISC",
  "main": "app/index.js",
  "name": "feature-flagons",
  "repository": "https://github.com/c10ckw0rk/feature-flag.git",
  "scripts": {
    "build": "npm run clean && npm run build:dev && npm run build:prd",
    "build:dev": "gulp build",
    "build:prd": "gulp dist",
    "clean": "rimraf dist",
    "lint": "eslint src",
    "prepublish": "npm run build",
    "test": "karma start --single-run --no-auto-watch karma.config.js"
  },
  "version": "1.0.4"
}
